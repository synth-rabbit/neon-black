Per-package register additions. Each package wrote one `<WP>.md` here instead of editing the shared registers (agent preamble, "Registers"); WP6 merged WP2–WP5 and WP8 merged WP7a–c into `names.md`, `build-choices.md`, `open-questions.md`, `conflict-register.md` and `changelog.md`, renumbering package-local ids (`tools/wp6_idmap.json`, `tools/wp8_idmap.json`). The originals are in git history; this folder now holds only this note. A future package appends here again and the next integration pass merges.
